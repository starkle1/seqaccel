% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hello.R
\name{load_accel_csv}
\alias{load_accel_csv}
\title{Load 3D time series and compute Euclidean norm}
\usage{
load_accel_csv(file, x = 3, y = 4, z = 5, time = 2, time_origin = "1970-01-01")
}
\arguments{
\item{file}{Path to the CSV file.}

\item{x}{Name or column index of the x-axis.}

\item{y}{Name or column index of the y-axis.}

\item{z}{Name or column index of the z-axis.}

\item{time}{Name or column index of the time variable.}

\item{time_origin}{Origin used if time is numeric
(passed to \code{\link[=as.POSIXct]{as.POSIXct()}}'s \code{origin} argument).}
}
\value{
A data.frame with an extra column \code{accel}.
}
\description{
Reads a CSV file with an index column, a time column,
and three coordinate columns (x, y, z). Converts the
time column to POSIXct and adds a new column \code{accel}
with the Euclidean norm sqrt(x^2 + y^2 + z^2).
}
\examples{
\dontrun{
df <- load_accel_csv("Example_window.csv", x = 3, y = 4, z = 5, time = 2)
head(df)
}

}
